# Demo script: PLAN -> DISRUPT -> RECOVER

Placeholder — fill in once the full loop (Milestone 3) is wired up.

1. **PLAN**: show the command center with an already-optimized block
   plan for the synthetic corridor; call out a couple of scheduled
   blocks and why they landed where they did (explainability panel).
2. **DISRUPT**: trigger a `DisruptionEvent` (e.g. an asset failure that
   makes a track segment unavailable mid-plan).
3. **RECOVER**: show automatic re-optimization producing a new feasible
   plan, and walk through what changed and why — tie back to the KPIs
   and audit trail so the judges see this is a deterministic,
   explainable decision, not an opaque AI-generated one.
