# Milestone 1 dependencies only (contracts + optimizer + script).
# FastAPI/ML/frontend deps join this file (or backend/pyproject.toml)
# in the next wave - see docs/architecture.md.
pydantic>=2.6
ortools>=9.9
pytest>=8.0
